[project]
name = "bitzaBackend"
version = "0.1.0"
description = "Backend for storing bitza this and bitza that (electronics components)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.17.2",
    "fastapi>=0.128.0",
    "sqlalchemy>=2.0.45",
]
